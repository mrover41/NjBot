# About project

It`s a discord bot on C

# How to compile this

To compile main project use this command:
`gcc main.c -o main.bin -ldl`

To compile module use this:
`gcc -shared -fPIC ./mod_dev/module_project_name/module_file.c -o modules/module_name.so`
